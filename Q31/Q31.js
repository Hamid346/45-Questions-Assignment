var userName = ["Admin", "Rida", "Naeem"];
if (userName.length === 0) {
    console.log("We need to find some users!");
}
else {
    for (var i = 0; i < userName.length; i++) {
        console.log(userName[i]);
    }
}
userName = [];
if (userName.length === 0) {
    console.log("We need to find some users!");
}
else {
    for (var i = 0; i < userName.length; i++) {
        console.log(userName[i]);
    }
}
