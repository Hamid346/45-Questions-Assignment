let userName: string[] = ["Admin", "Hamid", "Naeem"];

if (userName.length === 0) {
    console.log("We need to find some users!");
} else {
    for (let i = 0; i < userName.length; i++) {
        console.log(userName[i]);
    }
}

userName = [];
if (userName.length === 0) {
    console.log("We need to find some users!");
} else {
    for (let i = 0; i < userName.length; i++) {
        console.log(userName[i]);
    }
}